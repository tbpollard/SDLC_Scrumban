interface:
  display_name: "BA Project Intake"
  short_description: "Initialize a lightweight BA project workspace"
  default_prompt: "Use $ba-project-intake to initialize a local project workspace from the supplied project inputs."
