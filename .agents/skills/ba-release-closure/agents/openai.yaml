interface:
  display_name: "BA Release & Closure"
  short_description: "Record releases or complete project closure"
  default_prompt: "Use $ba-release-closure to record this release and change ticket, or assess whether the project can close."
