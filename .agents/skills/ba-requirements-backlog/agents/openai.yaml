interface:
  display_name: "BA Requirements & Backlog"
  short_description: "Turn discovery inputs into Jira-ready work"
  default_prompt: "Use $ba-requirements-backlog to turn discovery collateral into scoped Jira-ready requirements."
