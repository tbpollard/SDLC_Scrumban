interface:
  display_name: "BA Requirements & Backlog"
  short_description: "Generate Jira CSV and requirements approval"
  default_prompt: "Use $ba-requirements-backlog to create the fixed backlog, generate the Jira CSV, and manage requirements approval."
